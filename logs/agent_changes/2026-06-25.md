# Agent Change Log - 2026-06-25

This file documents non-trivial agent changes made on 2026-06-25.

---

## 17:35 - Add guided DPIR feasibility notebook for Step 5

Agent/User:
- Codex

Files changed:
- `.gitignore`
- `src/step5_dpir_feasibility.ipynb`
- `logs/agent_changes/2026-06-25.md`

What changed:
- Added `external/` to `.gitignore` so the external DPIR repository and pretrained model weights can remain local and untracked.
- Added a guided Step 5 notebook that explains DPIR, checks the Python/PyTorch environment, checks for `external/DPIR`, checks for `drunet_color.pth`, loads one project image, imports DPIR's `UNetRes`, constructs the color DRUNet model, and optionally performs a small shape-only forward pass if weights are available.
- The notebook intentionally stops before full deblurring so the first milestone is feasibility and model loading.

Why it changed:
- Step 5 requires trying a recent deblurring method, and the team wants to learn through a commented notebook rather than receiving a finished black-box implementation.

Expected benefit:
- Provides a safe, incremental DPIR setup workflow without committing heavy external code or weights.
- Creates clear learning checkpoints before implementing one-image DPIR restoration.

Risk / side effects:
- The notebook depends on cloning the official DPIR repository into `external/DPIR` and downloading compatible pretrained weights; if those external resources are unavailable, only the environment and image-loading cells will run.
- Full DPIR restoration is not implemented yet by design.

Validation:
- Validated the notebook JSON can be parsed.
- Parsed all notebook code cells with Python `ast.parse`.
- Ran a lightweight environment/image-loading smoke check: PyTorch 2.5.1 is available, MPS was detected, 3 clean images were found, and one clean image loaded as a 512 x 512 RGB array.
- Confirmed `external/DPIR` is currently missing, so the notebook will correctly stop at the DPIR setup checkpoint until the repository is cloned.

## 18:20 - Add final Step 5 DPIR deblurring notebook

Agent/User:
- Codex

Files changed:
- `src/step5_dpir_final.ipynb`
- `logs/agent_changes/2026-06-25.md`

What changed:
- Added a guided final Step 5 DPIR notebook using inverse-problems notation `g = A f + epsilon`.
- The notebook loads the external DPIR model, runs DPIR on all existing `data/processed/blur+noise` images as reflective-padding/circular-convolution model-mismatch cases, and creates one matched FFT/circular-convolution example.
- Added learning TODOs around filename matching, noise-level inference, tensor shapes, rho/sigma schedules, runtime/iteration effects, and boundary-condition model mismatch.
- Configured outputs to be saved under `data/results/dpir/` with filenames distinguishing existing mismatch outputs from the circular matched example.

Why it changed:
- Step 5 requires applying a recent method, and the team wants a notebook that teaches DPIR while producing visual deblurring outputs without yet comparing to Step 3 or Step 4 methods.

Expected benefit:
- Provides a runnable and explainable DPIR workflow for all existing degraded project images plus one controlled no-mismatch example.

Risk / side effects:
- Full DPIR restoration may be slow on CPU/MPS because it runs several 512x512 RGB images.
- Existing project images remain model-mismatch cases because they were generated with reflective padding while DPIR assumes circular FFT convolution.
- The notebook depends on the ignored external DPIR repository and `drunet_color.pth` weights.

Validation:
- Validated the notebook JSON can be parsed.
- Parsed all notebook code cells with Python `ast.parse`.
- Ran a smoke check confirming DPIR files and weights exist, 9 degraded images are matched to clean images, and the circular-convolution helper preserves image shape/range.
- Did not run the full DPIR restoration loop because it may be slow and should be run interactively in the notebook.

## 18:45 - Expand DPIR rho and sigma schedule explanation

Agent/User:
- Codex

Files changed:
- `src/step5_dpir_final.ipynb`
- `logs/agent_changes/2026-06-25.md`

What changed:
- Expanded the DPIR theory markdown to explain the data-consistency step, DRUNet denoising/prior step, and the roles of `rho` and denoiser `sigma`.
- Added a markdown explanation immediately before the schedule plotting cell and another explanation immediately after it interpreting what the plotted schedules mean.

Why it changed:
- The previous notebook did not explain the meaning of the DPIR `rho` and `sigma` schedules clearly enough for learning at the schedule-inspection step.

Expected benefit:
- Makes cell 11 understandable without needing to read the DPIR paper or source code first.

Risk / side effects:
- Markdown-only change; no DPIR execution logic was changed.

Validation:
- Validated the notebook JSON can be parsed.
- Parsed all notebook code cells with Python `ast.parse` after the markdown insertion.
