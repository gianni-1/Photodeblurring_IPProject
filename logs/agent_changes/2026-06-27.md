# Agent Change Log - 2026-06-27

This file documents non-trivial agent changes made on 2026-06-27.

---

## 00:00 - Convert Step 5 notebook TODOs into final explanatory notes

Agent/User:
- Codex

Files changed:
- `src/step5_dpir_final.ipynb`
- `logs/agent_changes/2026-06-27.md`

What changed:
- Replaced remaining TODO prompts in the Step 5 DPIR notebook with finished explanatory text and comments.
- Expanded the final "Stop here" cell with short notes about the role of the inverse model, DRUNet, boundary-condition mismatch, the circular matched case, and a fair report statement.

Why it changed:
- The notebook had been worked through and should now read as a completed explanatory artifact rather than an exercise sheet.

Expected benefit:
- Makes the notebook cleaner for review and easier to reuse in the report-writing phase.

Risk / side effects:
- Markdown/comment-only cleanup; no DPIR execution logic was changed.

Validation:
- Validated the notebook JSON can be parsed.
- Parsed all notebook code cells with Python `ast.parse`.
- Checked that no `TODO` strings remain in the notebook.

## 00:00 - Extend matched circular DPIR example to all noise levels

Agent/User:
- Codex

Files changed:
- `src/step5_dpir_final.ipynb`
- `logs/agent_changes/2026-06-27.md`

What changed:
- Updated the matched circular-convolution section to generate observations for the first clean image at noise levels `0.02`, `0.05`, and `0.10`.
- Updated the DPIR restoration cell to run the matched circular example for all three noise levels and save distinct outputs under `data/results/dpir/`.
- Updated the visualization cell to show one row per matched noise level and updated the final notes accordingly.

Why it changed:
- The matched circular-convolution example should mirror the three existing project noise levels while still using only the first image.

Expected benefit:
- Makes it easier to observe how DPIR behaves in the matched setting as the noise level increases.

Risk / side effects:
- Running cell 16 now performs three full 512x512 DPIR restorations instead of one, so it will take longer.
- Code changes only affect notebook workflow; reusable package code is unchanged.

Validation:
- Validated the notebook JSON can be parsed.
- Parsed all notebook code cells with Python `ast.parse`.
- Checked that the matched circular section now references all three noise levels and `matched_results`.
- Did not run the full DPIR restoration loop because it is intentionally interactive and may be slow.

## 00:00 - Add interpretation for matched circular DPIR reconstructions

Agent/User:
- Codex

Files changed:
- `src/step5_dpir_final.ipynb`
- `logs/agent_changes/2026-06-27.md`

What changed:
- Added a markdown interpretation after the matched circular-convolution reconstruction figure.
- The note explains how the DPIR result changes for noise levels `0.02`, `0.05`, and `0.10`, and connects the visual behaviour to denoising strength, detail loss, and inverse-problem stability.

Why it changed:
- The final reconstruction figure needed a written interpretation that can later be reused in the report.

Expected benefit:
- Makes the notebook more self-contained and explains the visual effect of increasing noise in the matched DPIR examples.

Risk / side effects:
- Markdown-only change; no DPIR execution logic was changed.

Validation:
- Validated the notebook JSON can be parsed.
- Parsed all notebook code cells with Python `ast.parse`.

## 00:00 - Add initial Step 4 sparse Matching Pursuit notebook

Agent/User:
- Codex

Files changed:
- `src/step4_sparse_matching_pursuit.ipynb`
- `logs/agent_changes/2026-06-27.md`

What changed:
- Added a guided Step 4 notebook for the sparse method using Haar wavelets and Matching Pursuit.
- Included inverse-problems notation, Haar sparsity demonstrations, crop loading, Haar transform helpers, coefficient decay visualization, and TODO scaffolds for zero-padding blur operators and Matching Pursuit dictionary construction.

Why it changed:
- Step 4 requires a sparse wavelet/Matching Pursuit method, and the team wants to begin with TODOs that can be filled in gradually.

Expected benefit:
- Provides a structured learning notebook for implementing and understanding the sparse method without attempting the full implementation at once.

Risk / side effects:
- The blur operators and full sparse reconstruction are intentionally incomplete and marked as TODOs.
- Placeholder functions raise `NotImplementedError` if called before implementation.

Validation:
- Validated the notebook JSON can be parsed.
- Parsed all notebook code cells with Python `ast.parse`.
- Confirmed the notebook contains TODO scaffolding for the remaining Step 4 implementation work.

## 00:00 - Align Step 4 notebook notation with exercise s09

Agent/User:
- Codex

Files changed:
- `src/step4_sparse_matching_pursuit.ipynb`
- `logs/agent_changes/2026-06-27.md`

What changed:
- Updated the Step 4 Matching Pursuit notebook to use the notation from `IP_Exercises_s09.pdf`.
- Replaced generic `f`/`g` naming with exercise-style notation such as `f_true`, `g_true`, `g'`/`gn`, `A`, `A_psf`, `Psi`, `Phi = A Psi`, `s`, and `list_r_norm`.
- Updated the Matching Pursuit markdown and scaffold signature to match `solve_matching_pursuit(A, gn, Psi, n)` from the exercise.

Why it changed:
- The team already solved this Matching Pursuit formulation in an exercise, so reusing the same notation should make Step 4 easier to implement and explain.

Expected benefit:
- Reduces notation mismatch between the project notebook and the earlier exercise sheet.
- Makes the remaining TODOs map directly to the exercise tasks.

Risk / side effects:
- Notebook remains intentionally incomplete; zero-padding operators and Matching Pursuit implementation are still TODO scaffolds.

Validation:
- Extracted notation and algorithm details from the provided exercise PDF.
- Validated the notebook JSON can be parsed.
- Parsed all notebook code cells with Python `ast.parse`.

## 19:33 - Start Step 3 Tikhonov deconvolution notebook

Agent/User:
- Pietro (`pietro-34`, commit `4811ce0247ed1043094c41fc9ab769792c3b99a9`)

Files changed:
- `scripts/operators.py`
- `src/photodeblur/step3.ipynb`

What changed:
- Added `apply_motion_blur_rgb`, a helper that applies the existing single-channel reflective FFT blur independently to all RGB channels and clips the result to `[0, 1]`.
- Added an initial Step 3 notebook for Tikhonov deconvolution with reflective boundaries, including the row-wise forward model, construction of a horizontal reflective blur matrix, consistency checks against the blur operator, the zero-order Tikhonov objective, a closed-form row/channel reconstruction routine, and L-curve parameter-selection notes.

Why it changed:
- Step 3 requires a standard method with parameter optimization, and the notebook starts the Tikhonov baseline in a way that matches the reflective-padding blur used for the processed images.

Expected benefit:
- Provides the foundation for the standard deblurring method and a reusable RGB blur helper for later experiments.

Risk / side effects:
- The Step 3 notebook includes local absolute paths from Pietro's machine and may need path cleanup before it runs on another machine.
- The standard-method workflow appears to be started but not yet fully finalized.

Validation:
- Not rerun while creating this retrospective log entry; details were reconstructed from commit `4811ce0247ed1043094c41fc9ab769792c3b99a9` and the changed file contents.
