# Agent Change Log - 2026-06-28

This file documents non-trivial agent changes made on 2026-06-28.

---

## 00:00 - Extend matched circular DPIR examples to all images

Agent/User:
- Codex

Files changed:
- `src/step5_dpir_final.ipynb`
- `logs/agent_changes/2026-06-28.md`

What changed:
- Updated the matched circular-convolution section of the Step 5 DPIR notebook to generate matched observations for all three clean images at noise levels `0.02`, `0.05`, and `0.10`.
- Updated the DPIR restoration cell to run all 9 matched cases and save distinct outputs under `data/results/dpir/`.
- Updated the visualization and interpretation text to describe matched circular results across all images and all noise levels.

Why it changed:
- The matched circular examples should cover all project images, not only the first one, so the DPIR behaviour can be inspected across different image content.

Expected benefit:
- Produces a more complete recent-method result set for Step 5 while keeping the matched circular setting separate from the reflective-padding mismatch cases.

Risk / side effects:
- Running the matched circular section now performs 9 full 512x512 DPIR restorations, so it will take longer than before.
- No reusable package code was changed.

Validation:
- Validated the notebook JSON can be parsed.
- Parsed all notebook code cells with Python `ast.parse`.
- Confirmed the notebook now generates 9 matched circular cases: 3 images times 3 noise levels.
- Did not run the full DPIR restoration loop because it is intentionally interactive and may be slow.

## 00:00 - Add zoomed DPIR result inspection cells

Agent/User:
- Codex

Files changed:
- `src/step5_dpir_final.ipynb`
- `logs/agent_changes/2026-06-28.md`

What changed:
- Added a zoomed-in inspection section to the Step 5 DPIR notebook.
- The new cells show crops for one existing reflective-padding/circular-convolution model-mismatch result and one matched circular-convolution result using the same crop coordinates.
- Added explanatory markdown describing how to interpret deblurring, denoising, oversmoothing, and artifacts in the zoomed views.

Why it changed:
- Zoomed crops make it easier to inspect local detail recovery and artifacts than full-image plots alone.

Expected benefit:
- Provides better visual evidence for the Step 5 report discussion and future method comparisons.

Risk / side effects:
- The zoom cells depend on `existing_results` and `matched_results` already being computed in earlier notebook cells.
- Markdown/code visualization only; no DPIR reconstruction logic was changed.

Validation:
- Validated the notebook JSON can be parsed.
- Parsed all notebook code cells with Python `ast.parse`.

## 00:00 - Complete Step 4 Matching Pursuit notebook implementation

Agent/User:
- Codex

Files changed:
- `src/step4_sparse_matching_pursuit.ipynb`
- `logs/agent_changes/2026-06-28.md`

What changed:
- Replaced most Step 4 TODO scaffolds with working code for Haar wavelet construction, zero-padding blur operators `A_1` and `A_2`, implicit Matching Pursuit, result plotting, sparse coefficient visualization, and residual stopping diagnostics.
- Implemented Matching Pursuit without explicitly storing the full dictionary `Phi = A Psi`, using the identity `Phi^T r = Psi^T A^T r` to compute atom correlations.
- Added interpretation text explaining sparsity, residual decrease, stopping criteria, and the relationship between the two padding variants.

Why it changed:
- Step 4 should be closer to a complete sparse-method notebook rather than only a TODO outline.

Expected benefit:
- Provides a runnable sparse reconstruction demonstration on a grayscale crop while preserving the exercise-sheet notation.

Risk / side effects:
- The notebook still runs on a crop rather than the full RGB images, because full explicit sparse reconstruction would be too expensive.
- Running 200 Matching Pursuit iterations for both operators may take noticeable time interactively.

Validation:
- Validated the notebook JSON can be parsed.
- Parsed all notebook code cells with Python `ast.parse`.
- Confirmed no `TODO` strings remain in the notebook.

## 00:00 - Extend Step 4 sparse notebook to all images and noise levels

Agent/User:
- Codex

Files changed:
- `src/step4_sparse_matching_pursuit.ipynb`
- `logs/agent_changes/2026-06-28.md`

What changed:
- Added an all-cases section to the Step 4 sparse Matching Pursuit notebook.
- The notebook now prepares all three clean images and all three existing noise levels, runs Matching Pursuit with both `A_1` and `A_2` on each crop, saves reconstruction crops, visualizes every case, and plots residual histories across all cases.

Why it changed:
- The sparse method should be demonstrated on all project images and noise levels, not only on one example crop.

Expected benefit:
- Provides a more complete sparse-method experiment set for report figures and qualitative comparison.

Risk / side effects:
- Running the all-cases section performs 9 cases times 2 operators, so it may take noticeable time interactively.
- The method still operates on grayscale crops rather than full RGB images for computational feasibility.

Validation:
- Validated the notebook JSON can be parsed.
- Parsed all notebook code cells with Python `ast.parse`.
- Confirmed the notebook contains the all-cases section and no `TODO` strings.
- Did not execute the full all-cases Matching Pursuit loop automatically because it is intentionally interactive and may be slow.

## 00:00 - Use matched observations for Step 4 sparse reconstructions

Agent/User:
- Codex

Files changed:
- `src/step4_sparse_matching_pursuit.ipynb`
- `logs/agent_changes/2026-06-28.md`

What changed:
- Updated the Step 4 sparse notebook so Matching Pursuit reconstructs from matched synthetic observations generated with the same zero-padding operator used during reconstruction.
- Reduced the demonstration crop to `64 x 64` and increased Matching Pursuit iterations to `800` to reduce overly blocky sparse reconstructions.
- Kept the existing reflective-padding crop only as a contrast/mismatch reference and updated explanatory text accordingly.
- Updated the all-images/all-noise-levels section to generate matched `A_1` and `A_2` observations for every case instead of using reflective-padding project images directly.

Why it changed:
- The previous plots looked misleadingly bad because the observation crop came from a full-image reflective-padding blur, while Step 4 reconstructed with local zero-padding operators. This model mismatch made the sparse reconstructions look worse for reasons unrelated to the Matching Pursuit method itself.

Expected benefit:
- Makes Step 4 a fairer demonstration of sparse wavelet Matching Pursuit and clarifies why the previous reconstructions looked unrealistic.

Risk / side effects:
- The sparse method is now demonstrated on synthetic matched crops rather than directly on the existing full-image reflective-padding degraded images.
- Running all cases still takes time because each case uses 800 greedy iterations for two operators.

Validation:
- Validated the notebook JSON can be parsed.
- Parsed all notebook code cells with Python `ast.parse`.
- Confirmed the notebook text explains the model mismatch and contains no `TODO` strings.

## 00:00 - Convert Step 4 sparse notebook to full RGB images

Agent/User:
- Codex

Files changed:
- `src/step4_sparse_matching_pursuit.ipynb`
- `logs/agent_changes/2026-06-28.md`

What changed:
- Reworked the Step 4 sparse Matching Pursuit notebook to operate on original full 512 x 512 RGB images instead of grayscale crops.
- Added fast recursive Haar transforms for full RGB arrays to avoid dense Haar matrix multiplication.
- Updated Matching Pursuit to select atoms over `(row, column, channel)` RGB coefficient indices.
- Updated the all-cases section to generate and reconstruct matched full RGB observations for all three images and all three noise levels.

Why it changed:
- The sparse method should match the project setting more closely by using full RGB images at original resolution.

Expected benefit:
- Provides full-size RGB sparse-method outputs and residual histories for the project report.

Risk / side effects:
- Full RGB Matching Pursuit is computationally expensive; running all cases may take a long time.
- Even thousands of selected atoms can still look sparse/blocky because the full RGB coefficient space has 786,432 possible atoms.

Validation:
- Validated the notebook JSON can be parsed.
- Parsed all notebook code cells with Python `ast.parse`.
- Confirmed the notebook no longer uses grayscale/luminance crop variables.

## 00:00 - Convert Step 4 sparse notebook to full-size grayscale

Agent/User:
- Codex

Files changed:
- `src/step4_sparse_matching_pursuit.ipynb`
- `logs/agent_changes/2026-06-28.md`

What changed:
- Reworked the Step 4 sparse Matching Pursuit notebook from full RGB to full-size grayscale/luminance images.
- Kept the original 512 x 512 spatial resolution while reducing the Matching Pursuit search space by a factor of three compared with RGB.
- Made `A_2`, the row-wise horizontal zero-padding operator, the main sparse reconstruction operator for all images and noise levels.
- Kept `A_1` as an optional single-case comparison instead of running it for every case.

Why it changed:
- Full RGB Matching Pursuit was too slow for interactive use, while grayscale preserves the original image size and is substantially faster.

Expected benefit:
- Provides a more practical sparse-method notebook that can run all three images and all three noise levels at full spatial resolution.

Risk / side effects:
- The sparse method no longer reconstructs color channels; it reconstructs luminance images for computational feasibility.
- Matching Pursuit can still be slow if `N_ITER` is set very high.

Validation:
- Validated the notebook JSON can be parsed.
- Parsed all notebook code cells with Python `ast.parse`.
- Confirmed the notebook uses full-size grayscale helpers and no full RGB Matching Pursuit solver remains.
