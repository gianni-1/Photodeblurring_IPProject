# Agent Change Log - 2026-06-24

This file documents non-trivial agent changes made on 2026-06-24.

---

## 17:25 - Complete Step 2 ill-posedness analysis setup

Agent/User:
- Pietro (`pietro-34`, commit `c62f74d`)

Files changed:
- `scripts/kernel.py`
- `scripts/operators.py`
- `src/step2.ipynb`

What changed:
- Added `motion_blur_psf` in `scripts/kernel.py` to create normalized linear motion-blur kernels from a blur length and angle.
- Added `apply_motion_blur` in `scripts/operators.py` to apply FFT-based blur with reflective padding to a single image channel.
- Added `src/step2.ipynb` for the Step 2 analysis, including PSF visualization, Fourier-response inspection, explicit small blur-matrix construction, a matrix-vs-convolution consistency check, singular-value plotting, and a Frobenius condition-number calculation.

Why it changed:
- Step 2 of the project plan requires explaining how severe the ill-posedness is. The Fourier response and singular-value analysis directly show attenuation of frequencies and instability of inverse filtering.

Expected benefit:
- Provides concrete figures and numerical evidence for the report section on ill-posedness.
- Starts moving reusable blur functionality out of the exploratory notebook into script-level code.

Risk / side effects:
- `scripts/kernel.py` and `scripts/operators.py` are useful modules, but they live under `scripts/`; later cleanup may move them into `src/photodeblur/` for package-style imports.
- The explicit blur matrix is built only on a reduced grid for analysis, so its condition number is an illustrative proxy rather than the full 512 x 512 RGB operator.
- The notebook path setup depends on the working directory and may need adjustment when run from a different location.

Validation:
- Not rerun while creating this retrospective log entry; details were reconstructed from commit `c62f74d` and the added source/notebook contents.
